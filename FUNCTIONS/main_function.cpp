#include <bits/stdc++.h>
using namespace std;
/* 
int main()
int main(int argc, char * argv[])
*/
main()
{
    //In C++ the main returns a value of type int by default

    return 0;
    // The explicit use of return 0 indicates that the program was successfully executed
    
}

/* Function prototyping: describes the function interface to the comiler by giving detais such as the number 
and type of arguments and the type of return values

type function-name(argument list)

     FUNCTION DECLARATION
eg: float average(int x,int y,float f)
    float average(int,int,float)
    
    FUNCTION DEFINITION
    float average(int x,int y,float f)
    {
        float avg=x+y+f/3;
    }

    FUNCTION CALL
    int n1=8,n2=6,num1,num2
    float n3=7.9,num3;

    num1=num2=4;
    num3=6.7;

    average(4,5,8.95);
    average(n1,n2,n3)
    float answer=average(num1,num2,num3)
*/

    
